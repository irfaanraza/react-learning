'use strict';

/**
 * process-testimonialpost service.
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::process-testimonialpost.process-testimonialpost');
