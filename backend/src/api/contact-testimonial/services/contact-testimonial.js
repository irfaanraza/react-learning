'use strict';

/**
 * contact-testimonial service.
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::contact-testimonial.contact-testimonial');
