'use strict';

/**
 * process-testimonial service.
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::process-testimonial.process-testimonial');
